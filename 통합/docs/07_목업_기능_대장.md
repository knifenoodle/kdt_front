# 목업 기능 대장

`uiux기획/mockup-v1.html` (2,369행)을 **이식 / 시연용 폐기 / Phase 2 보류** 로 분류한다.

🚨 `mockup-v1.html` 은 **읽기 전용 설계 원본 + 시각 회귀 기준**으로 원위치에 남긴다. 복사하지 않았다.

## 이식 완료

| 목업 요소 | 행 | 이식처 | 비고 |
|---|---|---|---|
| `LINES` (대사 전량) | 900-936 | `web/src/audio/lines.ts` + `server/app/deck/ownership_turn.json` | **신규 저작 0줄** — 검증된 15줄을 그대로 |
| 상태기계 `S`·`phase`·`RETRY_MAX` | 1493-1857 | `web/src/lib/session-machine.ts` | 전역 변형 → **순수 리듀서**. 불변식 S1·S2 를 테스트로 고정 |
| 캐릭터 SVG 11종 | 939-1133 | `web/src/assets/characters/*.svg` + `index.ts` | `캐릭터_에셋/svg/` 산출물을 복사. 생성 함수는 이식하지 않음 |
| 감정·상태 CSS | 183-330 | `web/src/components/Character.module.css` | `data-emo` 6종 · `data-state` 5종 · 키프레임 8종 실측 확인 |
| `:root` 토큰 | 21-48 | `web/src/tokens/index.ts` + `app/globals.css` | S-token 이 일치 검사 |
| `speak`/`shutUp` | 1470-1490 | `web/src/audio/speak.ts` | + `sanitizeForChild` 이중 방어 |
| `tone`/효과음 | 1431-1468 | `web/src/audio/sfx.ts` | Web Audio 오실레이터, 사운드 파일 0개 |
| `cantHear` 흐름 | 1782-1784 | `session-machine.ts` `CHILD_SPOKE_UNCLEAR` | 턴·재시도 미소비 |
| 지원 escalation | 1810-1826 | `session-machine.ts` `retry()` + `supportSlot()` | hint → sup1 → sup2 |

## Phase 2 보류

| 요소 | 행 | 사유 |
|---|---|---|
| 배경 씬 10종 | 1134-1424 | 1단계는 캐릭터만. `scene` 필드는 계약에 이미 있음 |
| 보상 연출 (`REWARDS`, `fxParty`/`fxPool`/`fxPound`) | 1873-1960 | 3턴 완주 이후 화면. 1단계는 turn 1 에서 멈춘다 |
| 무지개떡 6층 누적 | `S.filled` | 동일 |
| `PARTNER_ECHO` | 1614-1629 | 규칙 6 우회 방지 로직. `guards.ts` 로 옮길 예정 |
| `openHeart` | 1788-1809 | 마지막 턴 연출 |
| `nudgeSoon` 대기 유도 | 1667-1676 | S11 검사 대상이나 1단계엔 대기 구간이 없다 |
| 캐릭터 6종 상대역 로테이션 | 1521-1528 | 에셋은 반입됨. 선택 로직은 데크 확장과 함께 |

## 시연용 폐기 (제품 경로에 넣지 않음)

| 요소 | 행 | 사유 |
|---|---|---|
| 시연 바 (`force`, `nextWrong`) | 2240-2300 | 🚨 `uiux기획/CLAUDE.md:54` 규칙 8(한 화면 한 행동) 위반 |
| `s-cast` 캐릭터 시트 | 848-860, 2091-2136 | 동일. `/__dev` 로 옮길 수 있으나 1단계 미포함 |
| `s-scenes` 장소 그리드 | 861-870, 2225 | 동일 |
| `toast()` | 2295 | 보호자 게이트 스텁. 실물 ParentGate 로 대체 예정(G2-5) |

## 이식 시 발생한 변형

| # | 목업 | 통합 | 사유 |
|---|---|---|---|
| 1 | `who: '송편'` (한글) | `who: 'songpyeon'` (id) | 값이 `캐릭터_에셋/svg/` 파일명 stem 과 일치해야 에셋 해석이 가능하다. 한글 표시명은 `CHARACTER_NAME` 로스터가 담당 |
| 2 | `backEmo` | `back_emo` | snake_case — 백엔드 Pydantic 표현 가능성 유지 |
| 3 | (없음) | `lines.ai_disclosure` **신설** | `GOV-003-R1` 아동 대상 AI 고지. 목업에는 없었다 |
| 4 | SVG 인라인 생성 함수 | 정적 `.svg` + 생성 스크립트 | 🚨 `uiux기획/CLAUDE.md:121`(컴포넌트에 hex 리터럴 금지)과 충돌 → 에셋 계층으로 격리 |
| 5 | 전역 `S` 변형 | 순수 리듀서 | 안전 불변식을 기계 검사 가능하게 |
| 6 | `var LINES` (스크립트 내부) | `lines.ts` + 서버 `SessionScript` | 서버 데이터 주입 지점 확보 |

## 목업이 옳았고 그대로 지킨 것

- `:1609-1613` **"송편은 상황에 반응한다. 아이의 발화에는 반응하지 않는다"** → 불변식 **S1**
- `:1799` "감정은 joy 가 아니라 surprised — 채점이 아니라 목격이다" → S1
- `:1810-1826` 오답이 턴을 소비하지 않는다 → 불변식 **S2**
- `:1782-1784` `cantHear` 는 실패가 아니라 캐릭터의 부탁 → `lines.ts`
- `:1667-1676` 대기 유도 신호의 강도가 상승하지 않는다 → S11(Phase 2)
- `:1875` 결정론적 보상(`% length`) — 확률·희소성 없음 → 불변식 **S12**(동결)
- `:912` 유일한 평가성 발화가 칭찬 하나 → S3

**이 프로젝트에서 아동 안전이 가장 잘 검증된 자산은 목업이다.** 백엔드가 아니라.
`frontendbackend/CLAUDE.md` §3-2 의 안전 예외 조항(D6)이 이 사실을 규칙으로 만든 것이다.
