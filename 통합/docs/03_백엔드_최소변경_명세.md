# 백엔드 최소 변경 명세

배치 전략: **래핑(BFF) + 설정 1파일 벤더링 + 안전 결함 2건만 원본 수정.**

| 태그 | 의미 |
|---|---|
| **래핑** | `frontendbackend/server/` BFF 가 처리. 원본 무수정 |
| **원본수정** | D4 승인 2건. 사본 + `upstream-patches/CHANGELOG.md` 기록 |
| **벤더링** | 사본을 두고 모듈 속성 오버라이드 |
| **상류PR** | 원본 저장소에 제안. 권한 확인 필요 |

## 필수 (M1~M7) — 전부 완료

| # | 항목 | 원본 근거 | 처리 | 상태 |
|---|---|---|---|---|
| M1 | CORS | `main.py` 에 미들웨어 0건 | **래핑** — BFF 가 Next 산출물까지 같은 오리진에서 서브 → 문제 소멸. 개발 시만 `localhost:3000` 화이트리스트. 🚨 `allow_origins=["*"]` 금지 | ✅ |
| M2 | 카테고리 enum 검증 | `main.py:41`, `:58` 무검증 | **래핑** — 4값 `Literal`. 실측: `bogus` → 422 | ✅ |
| M3 | 오류 형태 정규화 | 422=배열 / 500·502=문자열 | **래핑** — `{ok,code,message_for_dev,correlation_id}` 단일 봉투 | ✅ |
| M4 | `scenarios: []` + 200 | `scenario_generator.py:143` | **래핑** — 오류로 승격하지 않고 저작 폴백으로 완성 | ✅ |
| M5 | 타임아웃 + 지연 은닉 | 스트리밍·타임아웃 없음, 실측 7~9초 | **래핑** — 20s 상한 + `asyncio.to_thread`. 프런트는 인트로 중 prefetch | ✅ |
| M6 | 헬스체크 | 없음 | **래핑** — `/api/health`. 🚨 키 값·앞자리·길이 반환 금지 | ✅ |
| M7 | 아동 식별정보 필드 부재 | `uiux기획/CLAUDE.md:107` | **래핑** — `extra='forbid'`. 실측 4/4 거부 | ✅ |

## 원본 수정 (D4 승인 2건)

| ID | 파일:행 | 변경 | 상태 |
|---|---|---|---|
| CS-004 | `knowledge_base/legal/LEGAL-003_…md:1-2` | 키보드 난타 2행 삭제 | ✅ |
| CS-003 | `knowledge_base/rules_index.json:97,109` | `draft` → `active` | ✅ |

실제 diff: 2 files, 3 deletions, 2 insertions. 그 외 0건.
사본·SHA-256·복원 명령: `compliance/upstream-patches/CHANGELOG.md`

## 벤더링 (1건)

`prompts/prompt_config.json` → `server/prompts/prompt_config.ttorang.json`

- **seam**: `rule_engine.scenario_generator.PROMPT_CONFIG_PATH` 모듈 속성 오버라이드
- **성립 근거**: `scenario_generator.py:21` 이 `from .config import PROMPT_CONFIG_PATH` 로 **import 시점 바인딩**
- **변경 내용**: `system_role`·`language_level` 만 만 4~6세로. `scenario_categories`·`user_instruction_template` 은 **원본과 바이트 동일**(가드 테스트가 검사)
- **취약점**: 상류가 `from . import config` 스타일로 바꾸면 조용히 7~9세로 복귀 → `test_guards.py::test_prompt_config_seam_attribute_exists`

## in-process 래핑 (1건)

`RuleEngine.get_applicable_rules` — CS-010 대응.
`build_rule_sections`(`rule_engine.py:34`)가 `top_k` 를 전달하지 않아 **호출자가 인자로 넘길 방법이 없다.**
원본 파일 수정 없이 프로세스 메모리에서만 기본값을 `max(8, 규칙수)` 로 올린다.

## Phase 2 상류 PR 후보

| 항목 | 근거 | 비고 |
|---|---|---|
| CS-002 fail-open 수정 | `validator.py:71-73` | severity 단일화 + 카테고리 위반 critical 승격 |
| CS-006 `_flatten_text` 재귀화 | `validator.py:29-30` | **옵션 (b) 전환의 선행조건** |
| `_REQUIRED_OUTPUT_FIELDS` 턴 트리 확장 | `validator.py:18` | 동일 |
| 규칙 6(오답 페널티 금지) 검사 신설 | `validator.py` | 동일 |
| `validator.py:14` ETH-002 severity 하향 | `critical` → `high` | CS-003 잔존 |
| 금칙어가 모델 컨텍스트로 재주입 | `validator.py:66` + `scenario_generator.py:136-141` | 블랙리스트 유출 |
| `issues[]` 에 `scenario_index` 추가 | `main.py:77-82` | 현재 매핑 불가 + 중복 누적 |
| `keywords`/`context_purpose`/`model`/`max_retries` HTTP 노출 | 이미 배선됨 | 최저비용 확장점 |
| `rule_loader.py:24-26` 침묵 실패 → 경고/예외 | CS-004 근본 | |
| `retriever.py:26` 배치 역전 | `LEGAL-003` 이 콘텐츠 생성에 강제 주입되고 `GOV-002` 는 한 번도 검색되지 않음 | 프라이버시 규칙이 무효한 곳에 있고 필요한 곳엔 없다 |
| `top_k` 기본값 상향 / always_include 컷오프 면제 | `rule_engine.py:21` | CS-010 근본 |
| 인증·레이트리밋 | `main.py:63-70` | G2-8 |
| 예외 텍스트 누출 차단 | `main.py:74-75` | 실측: SDK 원문 노출 확인 |
| `--reload` 안내 제거 | `사용가이드.md:188` 등 | |
| `language_level` 죽은 설정 정리 | `prompt_config.json:3` | 코드에서 읽히지 않음 |

🚨 **상류 PR 권한/의사 확인 필요.** 없으면 위 전부가 영구적 래핑 부채가 된다.
