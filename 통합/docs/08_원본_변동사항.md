# 원본 소스 변동 사항 (종합)

두 참조 소스에 우리가 무엇을 했는지 **전부** 여기 있다.

| 소스 | 절대경로 | 변경 |
|---|---|---|
| 백엔드 | `KDT 해커톤/백엔드/Communication_simulator` | **2개 파일 수정** (D4 승인) |
| UI/UX | `KDT 해커톤/uiux기획` | **변경 없음** (읽기 + 에셋 복사만) |

원칙(`frontendbackend/CLAUDE.md` §2): 원본은 읽기 전용. 아동 안전·규제 결함이 원본 파일 자체에 있는 경우에만,
① 사본 저장 ② CHANGELOG 기록 후 수정할 수 있다.

---

## A. 백엔드 — 수정 2건

git 기준: remote `https://github.com/KyungminChang/Communication_simulator.git`, 수정 전 HEAD `bdd1bc7`, working tree clean.

```
 knowledge_base/legal/LEGAL-003_개인정보보호법_아동특례.md | 2 --
 knowledge_base/rules_index.json                          | 4 ++--
 2 files changed, 2 insertions(+), 4 deletions(-)
```

### A-1. `LEGAL-003_개인정보보호법_아동특례.md:1-2` — 손상 문자열 제거 (CS-004)

**변경 전**
```
1: ㄴㅁㄴㅇㅇㅁㅁㅇㄴㄴㅇㅇㄴㅇㅇㄴㅇㄴㅇㅇㄴㅇㄴㄴㅇㅁㅁㅇㅇㅁㄴA
2: (빈 줄)
3: ---
4: id: LEGAL-003
```

**변경 후**
```
1: ---
2: id: LEGAL-003
```

**사유**: `rule_loader.py:19` `_FRONTMATTER_RE`가 선두 앵커라 `.match()`가 실패하고, `:24-26`이 **예외 없이** 전문을 body로 반환한다(침묵 실패). 실측: LEGAL-003만 `match=False`, LEGAL-001/002는 `True`.

**피해 범위 (정확히)**: 프롬프트 주입은 **일어나지 않았다** — `rule_engine.py:71-75` `_extract_rule_lines`가 `- **[` + `-R` 라인만 통과시켜 YAML·난타를 걸러낸다. 실제 피해는 ① 심사에서 프로젝트의 개인정보보호법 근거 문서 첫 줄이 키보드 난타로 보임 ② `retriever.py:28` 키워드 점수 오염 ③ 벡터 RAG 승격 시 `retriever.py:53` `page_content`에 난타+YAML 임베딩.

**검증**: 10개 규칙 문서 전부 `parsed=True, fallback=False`. 프롬프트에 난타·raw YAML 잔존 없음.

### A-2. `rules_index.json:97,109` — draft → active (CS-003)

```
ETH-002:  "status": "draft"  →  "status": "active"
GDL-001:  "status": "draft"  →  "status": "active"
```

severity는 **변경하지 않았다**(ETH-002 `high`, GDL-001 `medium` 유지). 전문가 자문 전이므로 상향 근거가 없다.

**사유 — 비대칭 제거**: `rule_loader.py:41`이 `status != "active"`를 건너뛰므로 이 두 문서의 규칙은 LLM 프롬프트에 **한 번도 도달하지 않았다.** 그런데 `validator.py:14`는 ETH-002 금지어로 생성물을 `critical` 처벌한다. **모델은 기준을 못 듣고 채점만 당하는 구조**였다.

프롬프트에서 누락되던 아동 안전 규칙:
- `ETH-002-R6` — 아동이 실패했을 때도 수치심을 유발하는 피드백이 아니라 재시도를 독려하는 톤
- `ETH-002-R4` — '정답'을 하나로 강요하지 않는다
- `ETH-002-R5` — 도움을 요청할 수 있는 어른이 있다는 메시지 포함
- `ETH-002-R1` — 가해 또래 캐릭터를 악마화하지 않는다
- `GDL-001-R1~R5` — 문장 15단어 내외, 학습목표 1개, 추상개념 지양, 반말 대화체, 단계적 난이도

**D1과의 관계**: 연령 적합성·언어수준을 규정하는 유일한 문서가 GDL-001이다. draft인 동안은 연령 톤 제어가 `age_range` 문자열 하나에만 의존했다.

**검증**: 활성 규칙 7건 → 9건. `build_rule_sections('ownership_turn')`에 `ETH-002-R1/R4/R5/R6`·`GDL-001-R1/R4` 도달 확인. 섹션 4,865자.

**🚨 부작용 (CS-010)**: 활성 규칙이 9건이 되어 리트리버 후보 풀이 정확히 8건 = `top_k` 경계에 도달했다. 현재 탈락 0건이지만 규칙이 하나만 더 active가 되면(CS-009: GOV-001) 점수 최하위가 조용히 사라진다. → `server/app/deps.py::_apply_top_k_wrap()`으로 in-process 완화.

### 복원 방법

```bash
cd "/Users/wonwoo_mac/Desktop/KDT 해커톤/frontendbackend"
cp "compliance/upstream-patches/LEGAL-003_개인정보보호법_아동특례.md.orig" \
   "../백엔드/Communication_simulator/knowledge_base/legal/LEGAL-003_개인정보보호법_아동특례.md"
cp "compliance/upstream-patches/rules_index.json.orig" \
   "../백엔드/Communication_simulator/knowledge_base/rules_index.json"
```

수정 전 SHA-256:

| 사본 | SHA-256 |
|---|---|
| `LEGAL-003_…md.orig` | `8dd6b16bc14417675ec1a1db849f63791a8d5a0d83562458715af77fe91b9b6d` |
| `rules_index.json.orig` | `87795d0ad520c06b94c3fe4248f5dc55b17845c48e8c429651b4ce2beb46e1c3` |

또는 `git checkout -- knowledge_base/` (원본 저장소에서).

---

## B. UI/UX — 변경 없음

`uiux기획/` 는 **한 바이트도 수정하지 않았다.**

`캐릭터연출_기획_v1.md:145/165/185`의 "사용자 결정 필요" 마커가 stale(같은 파일 `:638`에서 이미 전부 결정됨)이지만, D4 승인 범위가 백엔드 안전 결함 2건이므로 **수정하지 않았다.** 대신 `docs/02` §C와 `compliance/DECISIONS.md` D7이 오독 방지 장치 역할을 한다.

### 복사만 한 것

| 원본 | 사본 | 용도 |
|---|---|---|
| `캐릭터_에셋/svg/*.svg` (11개) | `web/src/assets/characters/*.svg` | 캐릭터 렌더링 |
| `mockup-v1.html:900-936` `LINES` | `server/app/deck/ownership_turn.json` + `web/src/audio/lines.ts` | 저작 3턴 데크. **신규 저작 0줄** |
| `mockup-v1.html:183-330` 감정 CSS | `web/src/components/Character.module.css` | `data-emo`/`data-state` 매핑 |
| `mockup-v1.html:21-48` `:root` | `web/src/tokens/index.ts` + `app/globals.css` | 디자인 토큰 |

**가져오지 않은 것**: `캐릭터_에셋/추출.mjs` — `vm.runInContext`로 HTML의 `<script>`를 실행하고 하드코딩 경로로 headless Chrome을 셸아웃한다. 통합 레포로 가져오지 않으면 리스크 0이므로 산출물(`svg/`)만 복사했다.

`mockup-v1.html` 자체도 복사하지 않았다. **읽기 전용 설계 원본 + 시각 회귀 기준**으로 원위치에 남긴다.

---

## C. 벤더링 1건

`prompts/prompt_config.json` → `server/prompts/prompt_config.ttorang.json`

| 키 | 변경 |
|---|---|
| `system_role` | "초등 저학년(7~9세) 전문 교육자" → "미취학 유아(만 4~6세) 전문 교육자" + 문장 길이·어휘 제약 추가 |
| `language_level` | 초등 1~3학년 → 만 4~6세 (⚠️ 이 키는 **코드에서 읽히지 않는 죽은 설정**이나 문서 정합성을 위해 갱신) |
| `scenario_categories` | **변경 없음** — 백엔드 절대 기준. 4키와 라벨 문자열 원문 유지 |
| `output_format` | **변경 없음** |
| `user_instruction_template` | **변경 없음** |

**seam**: `rule_engine.scenario_generator.PROMPT_CONFIG_PATH` 모듈 속성 오버라이드.
성립 근거는 `scenario_generator.py:21`이 `from .config import PROMPT_CONFIG_PATH`로 **import 시점 바인딩**한다는 점이다.

🚨 상류가 `from . import config` + `config.PROMPT_CONFIG_PATH` 스타일로 바꾸면 오버라이드가 **조용히 무력화되고 7~9세 프롬프트로 복귀**한다.
→ `server/tests/test_guards.py::test_prompt_config_seam_attribute_exists`가 감시.
→ `test_backend_category_keys_are_untouched`가 카테고리 4키·라벨의 바이트 동일성을 검사.

---

## D. in-process 래핑 1건 (파일 수정 아님)

`RuleEngine.get_applicable_rules` — CS-010 대응.

`build_rule_sections`(`rule_engine.py:34`)가 `top_k`를 전달하지 않아 **호출자가 인자로 넘길 방법이 없다.**
원본 파일 수정 없이 프로세스 메모리에서만 기본값을 `max(8, len(self.rules))`로 올린다.

구현: `server/app/deps.py::_apply_top_k_wrap()`
감시: `/api/health`의 `rules_dropped_by_top_k`, `test_guards.py::test_cs010_no_rules_dropped_by_top_k`

⚠️ **원본 `webapp/main.py` 경로에는 이 완화가 없다.** 원본 데모를 병행 시연하면 그쪽은 규칙이 탈락할 수 있다.

---

## E. 수정하지 **않은** 원본 결함

D4 범위(안전 결함 2건) 밖이므로 BFF 래핑 또는 상류 PR로 처리했다.

| ID | 항목 | 위치 | 처리 |
|---|---|---|---|
| CS-002 | validator fail-open (카테고리 위반 통과) | `validator.py:71-73` | 상류 PR 대기 |
| CS-005 | `category`/`age_range` 무검증 → `system_instruction` | `main.py:40-43`, `:58` | **BFF 래핑** |
| CS-006 | `_flatten_text` 중첩 미스캔 | `validator.py:29-30` | 상류 PR + (b) 전환 차단장치 |
| CS-008 | README "최초" 주장·무출처 통계 | `README.md:73,66,54,44` | **팀 조치** — `CLAIMS-SUBSTANTIATION.md` |
| CS-009 | GOV-001 빈 템플릿 | `GOV-001_…md:20,21,23,24` | **공고문 필요** — `GOV-001-FILLED.md` |
| — | 금칙어 사전 미탐/오탐 | `validator.py:11-15` | 프런트 `riskPatterns.ts`가 보정판 |
| — | 금칙어가 모델 컨텍스트로 재주입 | `validator.py:66` | 상류 PR |
| — | 인증·레이트리밋 부재 | `main.py:63-70` | **BFF 래핑** |
| — | 예외 원문 클라이언트 누출 | `main.py:74-75` | **BFF 래핑** (실측 확인됨) |
| — | `retriever.py:26` 배치 역전 | `LEGAL-003`이 콘텐츠 생성에 강제 주입, `GOV-002`는 미검색 | 상류 PR |

전체 목록: `docs/03_백엔드_최소변경_명세.md`

---

## F. 검증

원본 트리에 우리가 만든 파일이 없는지 자동 검사한다.

```python
# server/tests/test_guards.py::test_upstream_source_tree_is_not_written_to
forbidden = [".venv", "__pycache__/ttorang", "server", "web", "contracts", "compliance"]
```

현재 `git status --short` (원본 저장소):
```
 M knowledge_base/legal/LEGAL-003_개인정보보호법_아동특례.md
 M knowledge_base/rules_index.json
```

**신규 파일 0건. 삭제 0건. 그 외 수정 0건.**
