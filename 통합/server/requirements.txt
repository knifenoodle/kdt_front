# 또랑 BFF. 원본 백엔드 의존성을 상속한다(rule_engine 을 in-process import 하므로).
google-genai>=0.5.0
pydantic>=2.0
pyyaml>=6.0
fastapi>=0.110.0
uvicorn>=0.29.0

# BFF 전용
httpx>=0.27.0        # TestClient
pytest>=8.0
pytest-asyncio>=0.23
jsonschema>=4.0      # contracts/session-script.schema.json 대조
