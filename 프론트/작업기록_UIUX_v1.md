# 또랑 UI/UX 작업기록 v1 (2026.08.03)

무엇을 만들었는지가 아니라 **왜 그렇게 정했는지**를 남긴다. 결과물만 보면 알 수 없는
판단·되돌린 것·규칙 충돌이 이 문서의 핵심이다.

| 산출물 | 설명 |
|---|---|
| `mockup-v1.html` | 단일 HTML 클릭 프로토타입. 외부 요청 0건 |
| `캐릭터_가이드_v1.md` | 떡 캐릭터 8종 · 감정 · 장소 사양 |
| `CLAUDE.md` | 상위 규칙. 이 문서와 충돌하면 CLAUDE.md가 이긴다 (예외는 §7 참조) |

---

## 0. 시작 지점 — Task 01을 보류한 이유

원래 지시는 `task-01-tokens-and-primitives.md`(디자인 토큰 + 원시 컴포넌트 3종)였다.
계획을 제시한 뒤 사용자가 **"프론트 화면 목업부터 하고 싶다. 화면 구성과 동작 방법을 먼저"**
로 방향을 바꿨다.

**판단:** 토큰·컴포넌트는 화면이 확정된 뒤에 만드는 게 맞다. 화면이 바뀌면 컴포넌트 시그니처가
따라 바뀌므로, 순서를 뒤집으면 두 번 만든다. Task 01은 화면 확정 후로 미뤘다.

이전 목업(`MVP_UI_디벨롭/21_UI_컨셉_목업_v7.html`)은 **초등·텍스트 선택지 기반**이라
현재 방향(만 4~6세, 글자 못 읽음, 음성)과 맞지 않아 계승하지 않고 새로 짰다.

---

## 1. 캐릭터 — 떡 의인화

사용자가 떡 모티브 콘셉트(송편·인절미·가래떡·시루떡)를 제시했다.

**채택 이유:** 떡은 **형태만으로 개체가 구분된다.** 반달/네모/길쭉/층층 — 색을 지우고
실루엣만 남겨도 누가 누군지 안다. `CLAUDE.md` 규칙 4(글자 없이 작동)를 캐릭터 레벨에서
자동으로 만족한다.

### 되돌린 것: 스킬 : 캐릭터 1:1 매핑
원안은 캐릭터마다 담당 스킬을 고정하는 구조였다. **스킬이 늘 때마다 캐릭터가 늘어나
확장에서 터진다.** → **파트너 1명 고정(송편) + 상대역 교체** 2계층으로 바꿨다.
아이 입장에서 "내 편은 항상 같은 애"라는 안정감도 생긴다.

### 추가한 캐릭터 4종
꿀떡(부탁·도움요청) / 약과(사과·화해) / 백설기(진정) / 무지개떡(보상 오브젝트).
꿀떡은 **가장 작게** 그려서 아이보다 어린 존재로 읽히게 했다 — 아이가 도와주는
역방향 연습까지 한 캐릭터로 커버된다.

---

## 2. 화면 흐름

```
시작 → 상황 제시 → 말하기(마이크) → 기다리기 → 상대 반응 → [3턴] → 보상 → 종료
                                                  └ 무응답 15초 → cantHear
```

- **시나리오 카드를 나열하지 않는다.** 오늘 할 것 1개만 보여준다. 만 4~6세에게 선택지
  나열은 과부하다(규칙 8: 한 화면 한 행동)
- **다시 듣기 버튼은 항상 화면에 있다** (CLAUDE.md 8절: 지시를 한 번만 들려주지 않는다)
- **3턴이면 끝난다** (규칙 7). 파티가 붙은 뒤에도 이 원칙은 유지 — 파티는 클릭을 막지 않는
  오버레이일 뿐이고 `끝` 버튼은 계속 눌린다
- 마이크는 **탭 → 탭** 토글. push-to-talk 금지 — 이 나이는 누른 상태를 유지하지 못한다

---

## 3. 감정 레이어 — 기능 상태와 분리

사용자 요청: "인사이드아웃처럼 다양한 감정표현".

**핵심 결정: `data-state`(기능)와 `data-emo`(감정)를 다른 축으로 뒀다.**
`말하는 중`과 `슬픔`을 하나로 합치면 "말하면서 슬프다"가 불가능해진다.
분리한 덕에 상태 5개 × 감정 8개 = 40조합이 조합 폭발 없이 나온다.

### 되돌린 것 ①: 화남의 김(스팀)
머리 위 김 두 줄이 **도깨비 뿔로 읽혔다.** "시루떡은 악당이 아니다"와 정면 충돌 → 제거.
눈썹·입·볼 온기만 남겼다. 만화식 분노 마크·붉은 얼굴도 같은 이유로 금지 목록에 넣었다.

### 되돌린 것 ②: 모든 감정의 눈썹
사용자 요청으로 **눈썹을 화남에만** 남겼다. 떡에 눈썹이 있으면 사람 얼굴처럼 보여
떡의 인상이 흐려진다. 화남만은 눈썹 없이 성립하지 않아 예외.

> ⚠️ **남은 부작용:** 무서움과 놀람의 차이가 땀방울 하나로 줄었다. 두 감정을 같은 장면에서
> 연달아 쓰지 말 것.

### 감정은 아이를 향하지 않는다
규칙 6(오답 페널티 없음)의 연장. 시루떡이 화나거나 슬픈 대상은 **언제나 상황**이다.
아이 발화 뒤의 감정은 **누그러지는 방향으로만** 전개한다 (화남→놀람→무덤덤→기쁨).
반대로 악화시키면 대사 한 줄 없이 규칙 6을 어기게 된다.

---

## 4. 배경 장소 8곳 (→ 4차에서 10곳)

"실제 현실처럼 보이게" 요청. **사진은 못 쓴다** — 외부 이미지·CDN 0건 원칙 때문.
대신 인라인 SVG로 알아볼 수 있는 수준까지 그렸다.

키즈카페(기본) / 편의점 / 무인문방구 / 생활용품점 / 백화점 / 영화관 / 풋살장 / 태권도장

**판단 3가지**
1. **배경 채도를 낮추고(`saturate(.8)`) 위아래 스크림을 깔았다.** 배경이 시선을 뺏으면
   그 순간 이 앱은 실패한다. 주인공은 캐릭터다
2. **영화관도 검정을 면적색으로 쓰지 않았다** (3절). 짙은 남보라 `#6B6894`로 대신
3. **'다이소'를 「생활용품점」으로 적었다.** 노란 가격표·좁은 통로 같은 특징은 살리되,
   목업이라도 실존 브랜드 이름을 화면에 박지 않는다

첫 렌더에서 키즈카페·백화점·태권도장의 **벽면이 비어** 장소로 안 읽혀서, 무지개 벽화·
가랜드·쇼윈도 마네킹·현수막을 추가로 채웠다.

---

## 5. 보상 — 무지개떡과 꿀파티

### 무지개떡 (연습 1회 = 1층)
**숫자·점수·스탬프·스트릭을 쓰지 않는다** (8절). 층이 색으로 채워지는 것만으로 진행도가
읽힌다. 다 채우면 `끝`을 누를 때 0층으로 리셋되고 새 판이 시작된다 — 다 모은 상태로
남겨두면 이후 모든 세션이 파티로 끝나서 의미가 닳는다.

### 꿀파티 (6층 완성 시에만)
떡을 다 쌓았으니 꿀을 붓는다.

| 요소 | 연출 |
|---|---|
| 꿀단지 | 팝 하고 나타나 기울며 붓는다. 주기적으로 더 기울어진다 |
| 꿀 코팅 | 무지개떡 맨 위에 얹히고 옆으로 흘러내린다 |
| 꿀수영장 | 화면 하단이 꿀 웅덩이. 물결이 흐른다 |
| 물놀이 | 꿀떡·송편·인절미·시루떡이 꿀에 몸을 담그고 통통 뜬다. 꿀떡·인절미는 튜브 |
| 꿀폭죽 | 섬광 + 퍼지는 링 + 입자 20개. 처음 4발 연달아, 이후 2.6초마다 |
| 꿀비 | 방울 26개가 각기 다른 속도로 낙하 |

**되돌린 것 ①: 꿀 코팅이 무지개떡 위 두 층을 덮었다.** "다 모았다"가 안 읽힌다 →
맨 위에만 얹히고 흘러내리는 폭으로 축소.

**되돌린 것 ②: 꿀 줄기가 화면을 세로로 갈랐다.** 단지에서 수영장까지 720px 이어지니
화면이 두 동강 났다 → 단지 입구 아래 168px로 축소.

**되돌린 것 ③: 폭죽 입자를 흰색으로 했더니 밝은 배경에 묻혔다.** 처음엔 꿀비와 색이
겹쳐서 안 보였고, 흰색으로 바꾸니 배경에 묻혔다 → **꿀비보다 진하고 채도 높은
호박·주황**으로 착지. 섬광과 링을 함께 써서 형태로도 구분되게 했다.

---

## 6. 송편 색 선택 (아이가 직접)

실제 송편이 쑥·흰·분홍인 것을 그대로 쓴다. 시작 화면에서 아이가 탭으로 바꾼다.

- 스와치는 **88×88**, 단일 탭, **글자 없음** — 색깔 송편 모양 자체가 설명이다
- CSS 변수(`--song-body`)만 바꾸므로 모든 화면의 송편이 즉시 함께 바뀐다
- 선택 표시는 노란 테두리. 회색 처리·비활성 상태 없음

---

## 7. CLAUDE.md 규칙을 어긴 곳 (딱 하나)

> **3절 — "성별 고정 색(분홍/파랑)으로 캐릭터를 나누지 않는다. 분홍은 남아의 최대 기피색"**

**사용자가 이 건에 한해 명시적으로 허용했다.** 기록해 둘 근거와 리스크:

- **허용 근거:** 원래 규칙이 막으려던 것은 *서비스가 성별로 색을 배정하는 것*이다.
  여기서는 **아이가 3개 중 직접 고른다.** 배정이 아니라 선택이므로 규칙의 취지를 벗어나지
  않는다는 해석이 가능하다. 기본값은 쑥색(녹색)이고 분홍은 선택지 중 하나일 뿐이다.
- **남는 리스크:** 유아 색 선호 조사에서 분홍이 남아 기피색 1위인 것은 그대로다.
  분홍을 **기본값으로 올리거나, 캐릭터 성별을 암시하는 방식으로 쓰면** 원래 문제가 그대로
  돌아온다. 지금 구조(기본 녹색 + 아이 선택)를 유지하는 조건에서만 안전하다.
- 이 예외는 **송편 몸통색에만** 적용된다. 다른 캐릭터·UI 요소로 확대하지 않는다.

그 외 규칙은 전부 지켰다 — 터치 76/88, 단일 탭, 글자 없이 작동, 오답 페널티 없음,
3턴 종료, 검정·갈색·회색 면적색 금지, 광고 SDK·아이 계정 없음.

---

## 8. 검증 방법

정적 검사와 실제 실행을 둘 다 돌렸다.

```
JS 문법            node --check (HTML에서 스크립트 추출)
id 참조 무결성      $('...') 전수 대조
금지 제스처         onLongPress · PanResponder · touchmove · dblclick · draggable 부재
터치 타깃           76/88 미만 조작 요소 부재 (보호자 진입점 56만 예외)
외부 요청           0건
클릭 흐름           DOM 스텁 위에서 상태기계 전체 실행 (3턴 → 보상, 무응답 → cantHear)
시각 확인           헤드리스 크롬 스크린샷
```

> **주의:** 타임드 애니메이션(폭죽)은 정지 스크린샷으로 확인이 어렵다. DOM에 입자가
> 생성되는지 직접 세고, `--force-prefers-reduced-motion`으로 애니메이션을 끈 상태에서
> 렌더해 요소가 실제로 그려지는지 확인했다.

---

## 9. 시연 진입점

프레임 아래 시연 바(아이 화면 아님) 또는 주소 해시.

| 해시 | 화면 |
|---|---|
| `#cast` `#cast-joy` `#cast-angry` … | 캐릭터 8종 + 상태 5 · 감정 8 |
| `#scenes` `#scene-futsal` … | 장소 10곳 |
| `#party` `#pool` | 꿀파티 · 꿀수영장 (정상 흐름으로는 여러 세션 소요) |
| `#listening` `#thinking` `#canthear` | 대화 화면 특정 상태 |
| `#demo` | 시범(관람) 구간으로 바로 진입 |
| `#other-injeol` `#other-gara` `#other-yak` … | 상대역을 바꿔서 세션 시작 |
| `#pound` | 🫓 떡메치기 보상 |
| `#scene-class` `#scene-play` | 교실 · 놀이터 (5단계 실사와 겹치는 장소) |

시연 바 **「틀린 답」** 은 다음 발화를 오답으로 처리한다 — 재시도 루프가 턴을 소비하지
않는 것을 턴바로 확인할 수 있다.

**「글자 끄기」** 버튼은 규칙 4(글자 없이 작동) 검증용이다. 눌러서 화면이 여전히
이해되면 통과다.

---

## 10. 2차 작업 (같은 날 이어서)

### 완성 보상을 하나가 아니라 여러 종류로
초안은 6층을 다 모으면 꿀단지·꿀비·폭죽·수영장·물놀이가 **한 번에** 터졌다.
사용자 지적: *"다양한 선택지를 가져가려는 거지 한 번에 보여주려는 것이 아니다."*

맞는 지적이다. 한 번에 다 쓰면 **두 번째 완성에서 보여줄 게 없다.** 둘로 쪼갰다.

| | 성격 | 구성 |
|---|---|---|
| 🍯 꿀파티 | 떠들썩 | 꿀단지 + 꿀 줄기 + 꿀폭죽 + 꿀비 |
| 🏊 꿀수영장 | 잔잔 | 꿀 웅덩이 + 물결 + 캐릭터 물놀이 + 첨벙 소리 |

완성할 때마다 다음 연출로 순환한다. **수영장에 폭죽·꿀비를 섞지 않는 게 핵심** —
섞는 순간 두 연출이 같아진다. `REWARDS` 배열에 한 줄 추가하면 자동으로 순환에 낀다.

### 미구현으로 남겨뒀던 것 전부 구현
약과 화해(갈라졌다 조청으로 붙기) / 가래떡 늘어나는 말하기 / 꿀떡 꿀 비침 /
캐릭터 등·퇴장 트랜지션. 확인 방법은 `#cast` 에서 **「눌러보기」 배지 카드를 탭**.

등·퇴장을 넣은 이유는 미감이 아니다 — **즉시 나타나고 사라지면 만 4~6세는 등장을
놓치거나 소멸에 놀란다.** 세션 시작에 송편 → 180ms 뒤 상대역, 3턴 후엔 상대역이
퇴장한 다음 보상으로 넘어간다.

연타 대응도 넣었다. 클래스를 뗐다 붙일 때 **리플로우를 강제**해야 다시 재생된다.
아이는 반드시 연타하고, 그때 아무 일도 안 일어나는 게 가장 흔한 실망 지점이다.

### 캐릭터 에셋 보관
`캐릭터_에셋/` — SVG 11종 + 투명 PNG 11종 + 캐릭터 시트 1장 + **재생성 스크립트**.

핵심은 마지막이다. `추출.mjs` 는 `mockup-v1.html` 의 SVG 함수를 **그대로 실행**해서
뽑는다. 손으로 두 벌 관리하면 반드시 어긋나므로, 목업을 고치면 명령 한 번으로 따라오게 했다.

뽑을 때 `.kk-drip`(꿀떡 방울)과 `.honey-coat`(무지개떡 꿀)이 새어 나왔다 — 목업 CSS에서만
숨겨져 있고 독립 SVG에는 그 규칙이 없어서였다. 추출기의 기본 스타일에 추가했다.

> SVG 안에는 **표정 부위가 전부 들어 있고** idle·무표정 기준으로 일부만 켜져 있다.
> `display` 만 바꾸면 감정 8종을 그대로 재현할 수 있다. 조합표는 캐릭터 가이드 §5.

---

## 11. 3차 작업 — 연출 기획서 P0 4건

기획서 `캐릭터연출_기획_v1.md` 의 P0 항목을 구현했다. 순서는 의존성대로 —
화자 판정을 먼저 풀어야 시범 단계를 만들 수 있다.

### ① 화자 판정을 이름 문자열에서 떼어냈다 (가장 중요)

`setChar` 가 이렇게 돼 있었다.

```js
other.setAttribute('data-state', speaker==='시루떡' ? 'speaking':'idle');
```

**상대역이 시루떡이 아니면 입이 영원히 안 움직인다.** 소리만 나고 화면에서는
아무도 말하지 않는다. 글자를 못 읽는 아이에게 "지금 누가 말하는가"의 유일한 단서가
입 움직임이므로 이건 연출 문제가 아니라 기능 결함이었다. 예선 기획서 2단계가
상대역을 *"'시루떡', '인절미' 등"* 으로 못박고 있어 곧 터질 자리였다.

바꾼 방식은 목록 대조가 아니라 **배제**다.

```js
function actorOf(speaker){
  return (speaker===PARTNER || speaker==='a-song') ? 'a-song' : 'a-other';
}
```

송편은 교체되지 않는 파트너다. 그러니 **파트너가 아니면 전부 상대역**이다.
새 캐릭터를 추가해도 등록을 잊어서 깨질 곳이 없다 — 이게 목록 방식보다 나은 이유다.

곁들여 `OTHERS` 레지스트리와 `setOther(key)` 를 만들어 상대역을 실제로 갈아 끼울 수
있게 했고, 말풍선 이름도 같은 규칙을 타게 했다(`say()` 가 파트너가 아니면 현재
상대역 이름으로 자동 교정). 시연 바의 **「상대역 바꾸기」** 로 6종을 돌려볼 수 있다.

### ② 못 들었을 때 신호가 꺼지던 문제

검증하다 발견한 실제 결함이다. `cantHear` 는 이 두 줄에 걸려 있었다.

```
setChar:  mic.classList.toggle('idle', state==='idle')      → 맥동 꺼짐
CSS 185:  [data-state="idle"] .body{animation:breathe …}     → 호흡 멈춤
```

**아이가 다시 말해야 하는 바로 그 순간에** 행동 유도 신호 두 개가 동시에 사라진다.
`cantHear` 는 실패가 아니라 캐릭터의 부탁인데(CLAUDE.md 4절), 화면은 죽어 있었다.
둘 다 canthear 로 확장했다. 호흡은 3.0s — idle(3.4s)보다 살짝 빠르게 해서
안달은 나되 재촉은 아닌 쪽으로 뒀다.

### ③ 마음 열림 증폭

가이드 §2가 *"앱의 가장 강한 학습 지점"* 이라고 선언해 놓고 실제 연출은
**CSS transform 0.4초 이동 하나**였다. 목업 전체에서 의도와 구현의 격차가 가장 컸다.

셋을 얹었다.

1. **0.2초 정지 후 열림** — `.freeze` 로 모든 애니메이션을 멈췄다가 푼다.
   애니메이션의 기본기이고 비용이 0에 가깝다. **멈춘 뒤에 움직여야 "변했다"가 읽힌다.**
2. **개방음** `sfx.open()` — 392 → 523 → 659Hz 상행 3음. 실패음이 아니라 **개방음**이라는
   점이 중요하다(규칙 6). 소리는 모션이 아니므로 reduced-motion 에서도 살아남는다.
3. **팥고물 6알이 톡 흩어진다** — 기존엔 `.pat{opacity:.35}` 로 흐려지기만 했다.

⚠ 꿀폭죽과 성격을 반드시 갈라 놓았다 — **폭죽은 화면 전체·20알·섬광 있음,
여기는 캐릭터 국소·6알·섬광 없음.** 섞으면 두 연출이 같아진다.

그리고 갭 하나를 같이 막았다. `.opened` 는 `.lay1/.lay3/.pat` 를 움직이므로
**시루떡 SVG 전용**이었다. 상대역을 바꾸는 순간 "내 말이 상대를 바꿨다"를 보여줄
수단이 통째로 사라진다. 그래서 `openHeart()` 안에 **캐릭터 무관한 공통 개방 신호**
(정지 → 몸이 한 번 크게 부풀기 → 개방음)를 두고, 층 벌어짐은 그 위에 얹히는
시루떡 고유 연출로 남겼다. 턴 인덱스 하드코딩(`S.turn===2`)도 `length-1` 로 바꿨다.

**넣지 않은 것 — 송편의 반응.** 이 순간은 아이 발화 직후(`respond`)라, 송편이 기뻐하면
아이는 그걸 **"내 대답이 정답"** 으로 읽는다. 규칙 6이 금지한 정답 공개가 표정으로
우회된다. 시선 채널(`.pupil`)이 생겨서 **송편이 아이가 아니라 시루떡 쪽을 보고**
놀랄 수 있게 된 다음에 넣는다.

### ④ 시범 단계 — 예선 기획서 2단계가 통째로 없었다

지금까지 아이는 아무 시범 없이 곧바로 말해야 했다. 인트로 뒤에 송편↔시루떡의
모범 대화 3줄을 넣었다. 흐름은 **인트로(상황) → 예고 → 시범 → 아이 차례**.

설계의 전부는 **"지금은 내 차례가 아니다"를 오해 없이 아는 것**이다.
주 신호는 **마이크** 하나로 잡았다. 이 나이대에 "내 차례인가"는 화면 톤이나 배치가
아니라 **누를 것이 살아 있는가**로만 판단되고, 필요한 두 상태가 이미 코드에 있었다
(`data-off="1"` / `.idle` 맥동).

| 채널 | 시범(관람) | 실전 |
|---|---|---|
| **마이크 (주)** | `data-off="1"` — 잠들어 있음, 맥동 없음 | 깨어나며 맥동 |
| 턴바 (보조) | 감춤 — **시범은 턴이 아니다** | 표시 |
| 화면 톤 (보조) | `saturate(.5)` | `saturate(.8)` |
| 배치 (보조) | 둘이 서로를 향해 기욺 | 둘 다 정면 |

⚠ 화면 톤을 **어둡게 하지 않았다.** 검정 면적색 금지(3절)에 걸린다. 어둡히는 대신
**채도를 뺐다.** 기존 `.scene{filter:saturate(.8)}` 값을 낮추는 것으로 끝났다.

시범은 `S.turn` 을 건드리지 않으므로 3턴 규칙(규칙 7)에 영향이 없다. 다시 듣기 버튼은
살려뒀다 — 마이크가 잠겨 있으니 활성 버튼은 여전히 하나뿐이라 규칙 8도 통과한다.
시범 중 다시 듣기를 누르면 시범을 다시 보여준다.

### 확인한 것

`node --check` → DOM 스텁 위 상태기계 완주(18/18) → 상태별 개별 렌더.

**측정 함정 하나.** `--dump-dom` + `getComputedStyle` 로 transform 을 읽었더니
시범을 벗어난 뒤에도 기울기가 남아 있는 것처럼 나왔다. 가상 시간에서는 transition 이
프레임을 못 받아 **계산값이 얼어붙는다.** 실제 스크린샷을 찍으니 정상이었다.
§8에 적은 것과 같은 교훈이다 — **애니메이션이 걸린 속성은 계산값을 믿지 말고 렌더로 본다.**

**본 검증은 「글자 끄기」 + `--force-prefers-reduced-motion` 동시 적용**이다.
글자도 없고 움직임도 없는 최악의 조건에서 관람과 실전이 구분되는가.
정적 차이 4개(마이크 명도·턴바 유무·배경 채도·기울기)가 남아 구분됐다.

에셋도 다시 뽑았다(`추출.mjs`). 새로 생긴 `.pat-burst` 가 그대로 새어 나와
시루떡 PNG에 팥알 6개가 박혔다 — §10의 `.kk-drip` 때와 같은 실수라 추출기 기본
스타일에 바로 추가했다. **목업 CSS에서만 숨긴 부위는 반드시 추출기에도 적어야 한다.**

---

## 12. 4차 작업 — P1 · P2 · 부록 3건

### ② 파트너 송편의 감정 동조 — 표에 든 두 개의 `none` 이 핵심

시루떡이 화를 내는데 송편이 같은 얼굴로 서 있으면 **아이 옆에 아무도 없는 것과 같다.**
그래서 동조를 넣되, 매핑에서 두 칸을 일부러 비웠다.

```
angry  → none   같이 화내면 아이가 둘 사이에 낀다. 중립이라야 "내 편"이 유지된다
scared → none   같이 무서워하면 아이의 불안이 증폭된다
```

나머지는 한 단계 약하게(`data-emo-soft` — 부속만 끄고 눈·입은 남긴다).

**그리고 이 항목의 진짜 내용은 동조가 아니라 그 경계다.**

> **송편은 상황에 반응한다. 아이의 발화에는 반응하지 않는다.**

동조는 `askTurn` **한 곳에서만** 일어난다. `respond`(아이 발화 직후)에서 표정이 바뀌면
시간적 근접성 때문에 아이는 예외 없이 그것을 **"내 대답에 대한 채점"** 으로 읽는다.
기뻐하면 정답, 시무룩하면 오답. 규칙 6이 금지한 정답 공개가 표정으로 우회되는 것이다.
검증 하니스에 이 불변식을 테스트로 박아 뒀다(§8, G 항목).

### ③ 감정 전이 — 크로스페이드가 불가능해서 깜빡임 뒤에 숨겼다

입·부속은 `display` 스왑이라 전이가 **물리적으로 불가능**하다. 그래서 페이드를 포기하고
**부위가 바뀌는 프레임을 눈꺼풀 뒤에 숨겼다.** 사람도 표정을 바꿀 때 눈을 깜빡인다 —
새 개념을 들이지 않는 우회로다. 감김 110ms → 그 사이에 교체 → 뜨기, 총 260ms.

**큐잉하지 않는다.** 만 4~6세는 원인과 결과를 짧은 시간창 안에서만 잇는다. 대사보다
표정이 늦게 도착하면 인과가 끊긴다. 새 감정이 들어오면 대사 중간이라도 즉시 교체한다.

> **여기서 실제 버그가 하나 났다.** 조기 반환 조건을 *현재 속성*으로 비교했더니
> "sad 예약 → 곧바로 none 요청" 순서에서 none 이 '이미 none'으로 착각해 반환하고,
> **취소됐어야 할 sad 예약이 뒤늦게 실행됐다.** 시범이 끝난 뒤 송편이 슬픈 얼굴로
> 굳어 있었다. 비교 대상을 **예약된 목표**로 바꿔 고쳤다.
> 비동기 상태 갱신에서 반복되는 함정이라 코드에 주석으로 남겼다.

### ④ 표정 채널 확장 — 무서움과 놀람이 땀방울 하나로 갈리던 문제

가이드 §5가 스스로 경고한 항목이다. 눈을 **4개 변수**(가로·세로·시선 x·y)로 쪼개
감정(크기)과 시선(방향)을 독립적으로 얹을 수 있게 했다. `transform` 선언 하나로는
두 축을 겹쳐 쓸 수 없기 때문이다.

| 채널 | 무서움 | 놀람 |
|---|---|---|
| 눈 가로 / 세로 | 1.32 / **0.74** | 1.45 / **1.45** |
| 눈 형태 | 아래에서 눌려 올라옴 | 온전한 원 |
| 입 | 오므린 작은 O | 크게 벌린 타원 |
| 부속 | 땀방울 | 없음 |
| 몸 | **작게 계속 떤다** | **한 번 크게 튄다** |

**차이 1개 → 6개, 그중 5개가 정적**이라 reduced-motion 에서도 구분된다.

**기획서가 제안한 `.lid`(눈꺼풀 도형)는 넣지 않았다.** 눈꺼풀은 몸통색이어야 자연스러운데
그러면 8개 SVG 전부에 몸통색을 물려야 해서 결합도가 커진다. 대신 `transform-origin` 을
위로 옮기고 세로를 눌러 **같은 도형으로 같은 인상**을 만들었다. 채널은 얻고 결합은 피했다.

**시선(`data-gaze`)은 부수 효과가 컸다.** 두 곳에서 쓴다 —
① 시범 모드에서 둘이 서로를 본다(= 둘 사이의 일)
② 마음 열림에서 송편이 **아이가 아니라 시루떡을 본다**(= 채점이 아니라 목격).
②가 없었으면 아래 ⑦(가)4를 넣을 수 없었다.

### ⑤ 생존 신호

- **깜빡임 비동기화** — 모든 SVG가 같은 틱에 주입돼 **일제히** 깜빡이고 있었다.
  갤러리 8종이 한 몸처럼 깜빡이면 인형 진열대로 보인다. 주기 3종(4.4/5.2/6.1s) +
  시작 시점을 흩뜨렸다
- **상태별 호흡** — 호흡이 `idle` 에만 걸려 있어서 **듣는 중·생각 중에는 캐릭터가 완전히
  정지**했다. listening 2.6s(집중) / thinking 4.2s(골똘히) / canthear 3.0s 로 나눴다
- **대기 변주** — 8초·16초에 송편이 고개를 한 번 기울였다 돌아온다

> **재촉과 초대를 가르는 기준을 한 줄로 정했다 — "이 신호가 5초 뒤에 더 강해지는가?"**
> 강해지면 재촉이다. 그래서 마이크 맥동은 2초 주기를 **끝까지 유지**하고, 변주는
> 1회성이며 세지지 않는다. 음성 독촉은 넣지 않았다.

### ⑥ 화면 전환 — 화면을 움직이지 않는다. 캐릭터가 데리고 간다

만 4~6세는 화면 전체가 슬라이드하면 시선의 기준점을 잃는다. **캐릭터가 시선 앵커**이므로
전환은 캐릭터가 지고 가고, 화면 교체는 그 뒤에서 조용히 일어난다(180ms 페이드 한 겹).

- 시작 → 대화: 시작 화면의 송편이 먼저 퇴장한 뒤 화면이 바뀐다
- 대화 → 보상: **송편도 퇴장한다.** 예전엔 상대역만 나가고 송편은 화면과 함께
  사라져서, 아이 입장에선 파트너가 증발한 셈이었다

규칙 5(100ms 내 반응)가 우선한다 — 탭음이 먼저 나가고 그다음 전환이 시작된다.

### ⑦(가)4 송편의 반응 — ④(b) 가 생겨서 이제 넣을 수 있다

마음 열림은 아이 발화 직후라 ②의 경계에 정면으로 걸린다. 두 가지로 피했다.
**감정을 joy 가 아니라 surprised 로** 두고(채점이 아니라 목격),
**시선을 상대역 쪽으로 돌려** 반응의 대상이 아이가 아님을 눈으로 못박는다.

### ⑦(나) 보상 3번째 — 떡메치기

앞의 둘은 **꿀**이 주인공이고 위(쏟아짐)와 아래(고임)를 이미 차지했다.
비어 있던 축은 **리듬 · 중앙 무대 · 캐릭터가 행위자**다. 이 셋을 채웠다.
0.9초 × 4박, 인절미가 떡메를 들고(인절미는 실제로 떡메로 쳐서 만드는 떡이다),
칠 때마다 콩고물 7알이 톡 튄다.

- **나눠먹기 기각** — 무지개떡이 사라진다. 6세션에 걸쳐 쌓은 **성취의 증거**를 없애는
  연출은 보상이 아니라 회수로 읽힌다
- **단체사진 기각** — 정적이라 보상감이 약하고, '사진 찍기'라는 개념 자체가 이 나이엔 얕다
- **꿀 코팅도 얹지 않았다**(`noCoat`). '완성 표식'이라 공통이지만, 꿀을 섞는 순간
  세 연출의 성격 구분이 무너진다. 떡메치기의 완성 표식은 **광택**이다
- 절구는 밝은 베이지 + 화면 면적 4% 미만. 갈색 면적색 금지의 취지는 어둡고 탁한 색이
  화면을 덮는 것이므로, 밝고 작으면 벗어난다 — 다만 이 판단을 문서에 남긴다

배치를 두 번 고쳤다. 처음엔 `potpop` 키프레임을 재사용했다가 **거기 들어 있던
`rotate(-16deg)` 까지 딸려 와 절구가 옆으로 누웠다.** 그리고 키프레임에 `opacity:0` 을
두었더니 애니메이션이 돌지 않는 환경에서 절구가 통째로 사라졌다 —
**"움직임이 없어도 보여야 하는 것"은 애니메이션에 존재를 맡기지 않는다.**

### 부록 3건 — 결정

| # | 질문 | 결정 |
|---|---|---|
| ① | 5단계 "갈등 심화" 영상은 규칙 6·§5 적용 대상인가 | **적용한다.** 단 덱을 폐기하지 않고 **순서를 바꾼다** |
| ② | 재시도 상한과 3단계 지원 방식 | **2회 · 함께 말하기** |
| ③ | STT 텍스트를 어디에 두는가 | **파이프라인 내부 + 보호자 리포트. 아이 화면 금지** |

**①** 심화 영상은 형식(점수·빨간 X)이 아니라 **기능(아이 발화 → 나빠짐)** 이 규칙 6의
금지와 일치한다. 만 4~6세는 자연적 귀결과 처벌을 구분하지 못해 "네가 그렇게 말해서
나빠졌다"로 읽는다. 그런데 덱 자신이 완충을 갖고 있다 — 5슬라이드의 "마스코트가 감정을
보듬고 조언". **순서가 전부다.**

```
아이 발화 → 심화 영상 → 위로              ✗ 인과로 읽힌다
아이 발화 → 마스코트 개입 → 가정형 예시    ✓ 설명으로 읽힌다
```

**②** 규칙 6(재시도가 손실이면 안 됨)과 규칙 7(무한 루프 없음)을 동시에 지켜야 한다.
목업에 이미 선례가 있었다 — `cantHear` 는 `S.turn` 을 올리지 않는다. 그대로 확장했다.
**턴은 소비하지 않되 상한 2회**, 상한에 닿으면 벌이 아니라 지원 수준을 올려 턴을 완결시킨다.

3단계를 **"선택지 제시"가 아니라 "함께 말하기"** 로 정했다. 글자를 못 읽는 아이에게
선택지를 보이려면 두 발화를 **소리로만** 제시해야 하는데, 그러면 아이가 둘을 동시에
기억해야 한다. 한 문장을 따라 하는 쪽이 확실히 쉽다. 따라 하기로 끝나지 않도록
마지막 단계에서만, 핵심 어구만 준다.

**③** 새 판단이 필요 없었다. 규칙 4와 `CLAUDE.md` 5절이 이미 이 배치를 지정하고 있다.

### §2-D 장소 2곳 추가 — 교실 · 놀이터

덱이 **5단계 실사** 배경으로 지목한 두 곳인데, 1~4단계 SVG 목록에는 없었다.
**연습에서 한 번도 가보지 않은 장소가 실전에서 처음 나오면 전이가 성립하지 않는다.**
가이드 §6이 장소를 구체적으로 그린 이유 자체를 무너뜨린다. 그래서 8곳 → 10곳.
확인용 그리드는 2열 → 3열로 바꿨다(스크롤을 만들지 않기 위해).

### 확인한 것

`node --check` → DOM 스텁 상태기계 **32/32** → 상태별 개별 렌더 → reduced-motion 렌더.

새로 박은 테스트 중 중요한 것 —
**"respond 구간에서 송편이 기쁨/뿌듯/슬픔으로 바뀌지 않는다"**. 이건 연출이 아니라
규칙 6의 불변식이라, 나중에 누가 무심코 `respond` 에 `setEmo('a-song', …)` 를 넣으면
테스트가 깨지게 해 뒀다.

**하니스가 제품 대신 자기 자신을 잡은 경우도 있었다.** 테스트 G가 실패했는데 원인은
`startSession` 이 `S.phase` 를 리셋하지 않아 **이전 테스트의 `waitForChild` 가 남은 것**
이었다. 제품 쪽 결함은 아니지만 상태가 정직하지 않은 건 맞아서 리셋을 추가했다.

**측정 함정 두 번째.** `getComputedStyle` 로 `.mortar` 의 크기를 읽었더니 계속
`scale(.6)`(애니메이션 0% 지점)이 나왔다. 가상 시간에서 CSS 애니메이션이 프레임을
못 받아 얼어붙은 것이고, 실제 스크린샷에서는 정상이었다. §11에서 transform 으로 겪은
것과 같은 함정이다 — **애니메이션이 걸린 속성은 계산값을 믿지 말고 렌더로 본다.**

CSS 명시도로도 두 번 물렸다. `[data-emo-soft="1"] .extras>*`(0,1,0)가
`[data-emo="sad"] .tear`(0,2,0)에 져서 **약한 감정인데 눈물이 그대로 나왔고**,
`#s-reward.fx-pound .rainbow` 가 `#s-reward.party-on .rainbow` 와 명시도가 같아
소스 순서에서 밀렸다. 전자는 `!important`(의도적 오버라이드 층), 후자는 클래스를
하나 더 걸어 해결했다.

---

## 13. 다음에 할 것

1. **Task 01 재개** — 화면이 확정됐으므로 토큰·원시 컴포넌트로 옮길 수 있다.
   ⚠ 이건 **다른 코드베이스**다(Expo/RN + TypeScript). 목업은 설계 원본으로 남는다
2. **5단계 트랙** — 실사 배경·또래 AI·결과 영상. 연계 원칙은 가이드 §9에 확정돼 있다
3. **보호자 영역** — 덧셈 게이트, 리포트 홈 (지금은 진입점만 있고 토스트만 뜬다).
   STT 텍스트 전문이 들어갈 자리가 여기다(부록 ③ 결정)
4. **나머지 5개 연습** — 인사·부탁·사과·차례·진정. 화자 하드코딩이 풀려 이제 가능하다
5. **실제 STT·LLM 연동** — 지금은 「틀린 답」 버튼으로 오답 경로를 시뮬레이션한다.
   `stopListening` 의 `S.nextWrong` 자리에 판정 결과가 들어가면 된다
6. **갭 2 본 해결** — 말풍선이 화자를 위치·색·꼬리 방향으로 구분하게. 지금은 이름
   자동 교정까지만 했고, 「글자 끄기」에서는 여전히 입 움직임이 유일한 단서다
