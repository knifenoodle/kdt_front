# 또랑 (Ttorang) — 유아 의사소통 연습 AI

만 4~6세가 AI 캐릭터와 소리로 대화하며 사회적 상황(인사, 부탁, 사과, 거절, 차례 지키기)을
연습하는 앱. 아이는 글자를 읽지 못한다는 전제로 전부 설계한다.

---

## 0. 먼저 확정할 것 (여기만 고치면 나머지가 따라온다)

```
스택      : Expo SDK 52 / React Native / TypeScript (strict)
스타일    : StyleSheet + 토큰 상수 (Tailwind·styled-components 쓰지 않음)
음성 출력 : expo-speech (ko-KR)
음성 입력 : expo-audio 로 녹음 → 서버 STT
상태      : zustand
테스트    : jest + @testing-library/react-native
```

> 웹으로 갈 거면 이 블록만 Next.js 15 App Router + CSS Modules 로 교체.
> 그 아래 규칙은 플랫폼과 무관하게 동일하게 적용된다.

## 1. 파일 구조

```
src/
  tokens/        color.ts  type.ts  space.ts  motion.ts  index.ts
  components/    Tap.tsx  Character.tsx  MicButton.tsx  Sparkle.tsx  ParentGate.tsx
  screens/       Talk.tsx  Listening.tsx  Reward.tsx  ParentHome.tsx
  audio/         speak.ts  record.ts  sfx.ts
  lib/           guards.ts
scripts/
  check-ui-rules.mjs
```

토큰은 `src/tokens` 밖에서 정의하지 않는다. 컴포넌트 파일에 하드코딩된 색·크기 값이
있으면 그건 버그다.

---

## 2. 절대 규칙 (위반 시 빌드 실패로 취급)

1. **터치 타깃**: 아이 화면의 모든 조작 요소는 **최소 76×76**, 주 행동(마이크·다음·시작)은
   **88 이상**. 보호자 화면은 예외로 56 허용 — 의도된 대비다.
2. **터치 간격**: 인접한 조작 요소 사이 **최소 16**.
3. **단일 탭만**: 드래그·스와이프·핀치·롱프레스·더블탭 금지. `PanResponder`, `Gesture`,
   `onLongPress` 를 아이 화면에서 쓰지 않는다.
4. **글자 없이 작동**: 텍스트를 전부 지워도 아이가 다음 행동을 알 수 있어야 한다.
   글자는 아이콘·음성 위에 덧입히는 보조 레이어다. 텍스트만으로 전달되는 정보는 없다.
5. **100ms 내 반응**: 모든 탭은 즉시 시각(눌림 깊이) + 청각(짧은 톤) 응답. 네트워크 대기
   중에도 로컬 반응이 먼저 나간다.
6. **오답에 페널티 없음**: 빨간 X, 점수 차감, 정답 자동 공개, 실패 사운드 금지.
   재시도를 권하는 캐릭터 반응으로만 처리한다.
7. **세션은 끝난다**: 무한 루프 없음. 대화 3턴이면 보상 화면 → 종료 안내.
8. **한 화면 한 행동**: 탭 바, 햄버거, 중첩 모달 금지. 아이 화면은 스크롤 없음.
9. **문자열 하드코딩 금지**: 아이에게 들려줄 문장은 전부 `src/audio/lines.ts` 에 모은다.

## 3. 색 — 역할로 나눈다

파스텔 일색으로 가지 않는다. 유아 선호 조사에서 고채도 원색(노랑·주황·초록)이 최상위,
검정·갈색·회색이 최하위였다. 파스텔 선호는 초등 이후에 나타난다.
따라서 **면적은 저채도, 행동 유도점은 고채도**로 분리한다.

```ts
// 배경·여백 — 저채도. 화면 대부분을 차지하므로 진정 효과 우선
bg:        '#F4F1FB'   // 옅은 라일락
surface:   '#FFFFFF'

// 행동 유도점 — 고채도. 아이 눈이 여기로 가야 한다
action:    '#FFC12E'   // 마이크, 시작
success:   '#2FB477'   // 잘했어
character: '#FF8A47'   // 캐릭터 본체

// 글자 — 검정 대신 남색 (검정은 유아 선호 최하위)
ink:       '#2B3A55'
inkSoft:   '#6B7B99'
```

- 검정·갈색·회색을 **면적색으로 쓰지 않는다.**
- 성별 고정 색(분홍/파랑)으로 온보딩이나 캐릭터를 나누지 않는다.
  분홍은 남아의 최대 기피색이다.
- 빨강은 오류 표시에 쓰지 않는다. 아이 화면에 오류 색 자체가 없다.

## 4. 캐릭터 상태 — 5개 (3개 아님)

행복 경로만 만들면 실제 사용에서 무너진다. 아래 5개를 전부 구현한다.

| 상태 | 트리거 | 표현 |
|---|---|---|
| `idle` | 대기 | 느린 호흡, 눈 깜빡임 |
| `listening` | 녹음 중 | 몸이 살짝 기울고 귀 쫑긋, 파형 링 |
| `thinking` | STT/LLM 대기 | 고개 갸웃 + 점 세 개. **3초 넘으면 "조금만 기다려줘" 음성** |
| `speaking` | TTS 재생 | 입 모양 + 말풍선 아이콘 |
| `cantHear` | 권한 거부 / 무음 / 인식 실패 | 손을 귀에 대고 "잘 안 들려, 다시 말해줄래?" |

`cantHear` 는 실패가 아니라 **캐릭터의 부탁**으로 표현한다. 아이 탓으로 만들지 않는다.

## 5. 보호자 게이트

- **롱프레스를 게이트로 쓰지 않는다.** 만 4~6세가 가장 잘하는 동작이다.
- 게이트는 **두 자리 덧셈 4지선다**. 매번 난수로 생성한다.
- 게이트 뒤에만 둘 것: 결제, 구독, 대화 리포트 전문, 사용 시간 설정, 계정, 데이터 삭제.
- 게이트 화면 자체는 **아이가 보기 지루하게** 만든다. 애니메이션·사운드 없음.

## 6. 법적 제약 (설계 단계에서 반영, 나중에 못 붙임)

- 만 14세 미만 → **법정대리인 동의 + 동의 확인** 절차 없이는 개인정보 처리 불가.
  아이 온보딩에서 이름·생년월일을 받지 않는다. 필요한 건 연령대(4/5/6세)뿐.
- **행동 기반 맞춤 광고 금지.** 광고 SDK를 넣지 않는다.
- **아이 계정 소셜로그인 금지.** 계정은 보호자 것 하나만 존재한다.
- 음성 녹음은 STT 처리 후 **즉시 파기**. 원본을 저장하지 않는다. 저장이 필요하면
  보호자 동의를 별도로 받고, 기본값은 저장 안 함이다.
- 아이에게 보이는 고지는 "이해하기 쉬운 양식과 명확하고 알기 쉬운 언어"로 — 사실상
  아이용 고지는 만들지 말고 보호자 화면으로 보낸다.

## 7. 검증

`npm run check:ui` 가 통과해야 PR을 올린다. `scripts/check-ui-rules.mjs` 가 검사하는 것:

- `src/screens/{Talk,Listening,Reward}.tsx` 에 `onLongPress` / `PanResponder` / `Gesture` 부재
- 아이 화면의 `Pressable` 은 전부 `Tap` 컴포넌트를 경유 (직접 사용 금지)
- 컴포넌트 파일에 `#RRGGBB` 리터럴 부재 (토큰만 사용)
- `minHeight` / `minWidth` 숫자 리터럴이 76 미만인 곳 부재

## 8. 하지 말 것

- 랭킹, 리더보드, 친구 비교 → 이 나이는 비교 개념이 형성되지 않았다
- 연속 출석 스트릭 압박 → 부모가 가장 싫어하는 패턴
- 아이 화면의 로딩 스피너 → 캐릭터의 `thinking` 상태로 대체
- 3~12세 통합 대응 → 만 7세 이상은 이 앱의 톤을 "애기 같다"고 거부한다
- 지시를 한 번만 들려주기 → 다시 듣기 버튼은 항상 화면에 있다
