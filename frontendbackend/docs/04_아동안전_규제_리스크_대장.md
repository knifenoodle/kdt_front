# 아동 안전·규제 리스크 대장

포맷: `frontendbackend/CLAUDE.md` §4-1. 1행 요약 인덱스는 `compliance/RISK-REGISTER.md`.

분류: `[1]` 배포 코드에 이미 존재 · `[2]` 기획 문서만 · `[3]` 통합이 신규 도입
**처리 순서 규칙 4**: `[3]`을 `[1]`보다 먼저 처리한다 — `[1]`은 기존 부채지만 `[3]`은 우리가 지금 만드는 위험이고 되돌릴 기회가 통합 시점뿐이다.

---

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-001
심각도   : HIGH
분류     : [3] 통합이 신규 도입
영역     : 유해성·안전
근거     : rule_engine/validator.py:11-15, scenario_generator.py:143, webapp/main.py:83
자사규칙 : LEGAL-001-R1, LEGAL-002-R2, ETH-002-R6, ETH-001-R4
현상     : 가드레일 전체가 20단어 부분문자열 블랙리스트다. 이를 통과한 LLM 문자열이
           글자를 못 읽는 만 4~6세의 귀로 TTS 를 통해 직접 들어간다. 현행 데모는
           화면 텍스트라 성인 개발자가 눈으로 걸러내지만 통합 후에는 검수자가 없다.
재현     : validator 격리 실행 —
           `죽고 싶을 만큼 힘들었어. 자해했어.`          → is_valid=True (자해 필터 없음)
           `옷 벗어봐. 야한 사진 보여줄게.`               → is_valid=True (성적 필터 없음)
           `우리 반 전체가 너랑 안 놀 거야. 아무도 말 걸지 마.` → is_valid=True (금지어 미사용)
           `장애물 달리기를 했어요`                        → is_valid=False (오탐)
영향     : 아동이 부적절한 문장을 음성으로 듣는다. 되돌릴 수 없다.
완화     : sanitizeForChild() 관문 + speak() 정의부 이중 방어 + S4 기계 검사
완화붕괴 : speak() 를 우회해 SpeechSynthesisUtterance 를 직접 만들면 붕괴한다.
           check-safety-rules.mjs 는 정규식 기반이라 동적 문자열 조립을 잡지 못한다.
필요조치 : ✅ 완료 — web/src/lib/sanitizeForChild.ts + riskPatterns.ts.
           위험·과길이 문자열은 저작 폴백으로 대체되고 아이는 차이를 느끼지 않는다.
           잔존: G2-6 실사용 분류기(LLM 2차 판정 병용)
게이트   : G1
상태     : 완료
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-002
심각도   : HIGH
분류     : [1] 배포 코드
영역     : 유해성·안전
근거     : rule_engine/validator.py:71-73, :50-55
자사규칙 : GDL-002-R1, ETH-001-R4
현상     : is_valid = (critical 없음) AND (OUTPUT_FORMAT 없음). 카테고리 위반은
           severity="high" 로 기록되어 두 조건 모두 비해당 → 통과한다(fail-open).
재현     : validate_scenario({... "category": "<img src=x onerror=1>" ...}) → is_valid=True
영향     : GDL-002-R1("모든 시나리오는 4개 카테고리 중 정확히 하나")이 집행되지 않는다.
           잘못된 카테고리로 생성된 시나리오가 정상 결과로 나온다.
완화     : Gemini response_schema 의 ScenarioCategory Enum (scenario_generator.py:52-56)
완화붕괴 : 사용가이드.md:223-224 가 AI 제공자 교체를 지원 시나리오로 안내한다 → enum 소실
필요조치 : ✅ 완료 (2026-08-05, engine/ 복사 전환으로 가능해짐) —
           is_valid 를 severity 기준으로 단일화(critical 또는 high → invalid),
           카테고리 위반을 critical 로 승격, rule_id 특례(OUTPUT_FORMAT) 제거.
           검증: test_guards.py::test_cs002_fail_open_is_fixed
게이트   : G1
상태     : 완료
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-003
심각도   : HIGH
분류     : [1] 배포 코드
영역     : 유해성·안전
근거     : knowledge_base/rules_index.json:97,109 · rule_loader.py:41 · validator.py:14
자사규칙 : ETH-002-R1/R4/R5/R6, GDL-001-R1~R5
현상     : rule_loader 가 status != "active" 를 건너뛰므로 ETH-002·GDL-001 이 draft 로
           프롬프트에 한 번도 도달하지 않는데, validator.py:14 는 ETH-002 금지어로
           critical 처벌을 한다. 모델은 기준을 못 듣고 채점만 당한다.
재현     : load_rules() → 활성 7건, ETH-002/GDL-001 부재.
           build_rule_sections('ownership_turn') 에 ETH-002-R6 미포함.
영향     : 프롬프트에서 누락되던 규칙 — ETH-002-R6(실패 시 수치심 유발 금지),
           R4(정답 강요 금지), R5(도움 요청 어른 언급), R1(가해 또래 악마화 금지),
           GDL-001-R1~R5(문장 15단어, 반말체, 단계적 난이도).
           특히 GDL-001 은 만 4~6세 언어수준을 규정하는 유일한 문서라
           D1(연령 하향) 결정을 규칙 엔진이 뒷받침하지 못했다.
완화     : 없었음
완화붕괴 : —
필요조치 : ✅ 완료 — rules_index.json:97,109 를 active 로 승격(D4 승인).
           실측: 활성 7→9건, ETH-002-R1/R4/R5/R6·GDL-001-R1/R4 프롬프트 도달 확인.
           ✅ GDL-001 본문도 2026-08-05 만 4~6세 기준으로 재작성됨(CHANGELOG 변경 #3).
           잔존: validator.py:14 의 ETH-002 severity 를 high 로 하향(상류 PR)
게이트   : G1
상태     : 완료 (잔존 1건 Phase 2)
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-004
심각도   : MED   (당초 HIGH 로 보고했으나 프롬프트 주입이 없음을 확인해 하향)
분류     : [1] 배포 코드
영역     : 개인정보(PIPA)
근거     : knowledge_base/legal/LEGAL-003_개인정보보호법_아동특례.md:1 · rule_loader.py:19,24-26
자사규칙 : LEGAL-003 (severity critical)
현상     : PIPA 근거 문서 1행이 키보드 난타(`ㄴㅁㄴㅇㅇㅁ…A`)다. _FRONTMATTER_RE 가
           선두 앵커라 .match() 가 실패하고, :24-26 이 예외 없이 전문을 body 로 반환한다(침묵 실패).
재현     : _read_markdown(LEGAL-003) → metadata={} , body=파일 전체. LEGAL-001/002 는 정상.
영향     : ⚠ 프롬프트 주입은 일어나지 않는다 — rule_engine.py:71-75 _extract_rule_lines 가
           `- **[` + `-R` 라인만 통과시켜 YAML·난타를 걸러낸다. 실제 피해는
           ① 심사에서 프로젝트의 개인정보보호법 근거 문서 첫 줄이 키보드 난타로 보임
           ② retriever.py:28 키워드 점수 오염
           ③ 벡터 RAG 승격 시 retriever.py:53 page_content 에 난타+YAML 임베딩
완화     : _extract_rule_lines 의 패턴 필터
완화붕괴 : 규칙 문서 본문 형식이 바뀌면 필터가 무력해진다
필요조치 : ✅ 완료 — 1~2행 삭제 + rule_loader.py:24-26 의 침묵 실패를 경고 로그로 전환.
           10개 문서 전부 파싱 성공(S13). 규칙 문서가 조용히 깨지는 것이 이 프로젝트에서
           가장 위험한 실패 양상이다 — 규칙은 사라졌는데 시스템은 정상으로 보인다.
게이트   : G1
상태     : 완료
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-005
심각도   : HIGH
분류     : [1] 배포 코드
영역     : 보안
근거     : webapp/main.py:40-43, :58 · rule_engine.py:59-68 · scenario_generator.py:90,113-117
자사규칙 : ETH-001-R4, GOV-003-R1
현상     : num_scenarios 만 검증되고 category·age_range 는 무검증 자유 문자열이다.
           f-string 을 거쳐 system_instruction 계층에 그대로 진입한다.
           2차 경로: scenario_generator.py:90 의 .get(category, category) 원문 폴백.
재현     : GET /api/rules?category=bogus → HTTP 200 + 그럴듯한 규칙 텍스트
           (contracts/backend-observed/rules.bogus.200.json)
영향     : 프롬프트 인젝션. 규칙 섹션 상단·하단 양쪽에 지시를 심을 수 있고,
           20단어 블랙리스트가 유일한 사후 방어다.
완화     : 원본에는 없음
완화붕괴 : —
필요조치 : ✅ BFF 래핑 완료 — 4값 Literal 강제, age_band 를 서버 정의 라벨로만 번역.
           실측: bogus → 422. 원칙: "사용자 문자열은 어떤 경로로도 system_instruction 에 도달하지 않는다."
           잔존: engine/webapp/main.py(레거시 데모)는 여전히 무검증. BFF 가 제품 경로다.
게이트   : G1
상태     : 완료 (BFF 경유 한정)
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-006
심각도   : HIGH (잠재 — 통합이 트리거)
분류     : [1] 배포 코드
영역     : 유해성·안전
근거     : rule_engine/validator.py:29-30
자사규칙 : LEGAL-001-R1, ETH-002-R6
현상     : _flatten_text 가 scenario.values() 중 isinstance(v, str) 인 top-level 값만 스캔한다.
재현     : validate_scenario({..., 'x': {'y': '칼로 협박했다'}}) → is_valid=True
영향     : 현행 스키마는 평면이라 무해하다. 그러나 **UI 세션 모델이 턴 배열**이다.
           스키마가 중첩으로 바뀌는 순간 아이가 듣는 15줄 전부에 금칙어 스캔이 돌지 않는다.
완화     : 1단계 어댑터가 (c) 저작 데크 방식이라 중첩 스키마를 백엔드에 요구하지 않는다
완화붕괴 : source.adapter 를 backend_session 으로 전환하는 순간 붕괴한다
필요조치 : ✅ _flatten_text 재귀화 완료 (2026-08-05, engine/ 복사 전환으로 가능해짐).
           dict/list/tuple/set/str 을 재귀 순회한다.
           검증: test_guards.py::test_cs006_nested_fields_are_scanned
           잔존: _REQUIRED_OUTPUT_FIELDS 턴 트리 확장 + 규칙 6 검사 신설
                 → (b) backend_session 어댑터 전환 시 필요
게이트   : G1
상태     : 완료 (재귀 스캔) / 잔존 2건은 (b) 전환 선행조건
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-007
심각도   : MED (1단계) / CRITICAL (STT 활성화 시)
분류     : [2] 기획 문서만 → STT 구현 시 [1] 로 승격
영역     : 평판
근거     : knowledge_base/legal/LEGAL-002_학교폭력예방법_정의.md:29
자사규칙 : LEGAL-002-R4, ETH-002-R5, ETH-002-R6
현상     : "실제 학교폭력 정황 감지 시 에스컬레이션/신고 연계 절차는 별도 정책 문서로
           수립 필요"라고 명시하는데 그 문서가 두 소스 어디에도 없었다.
           아동이 마이크에 실제 피해·자해를 말했을 때 분기가 코드에도 스펙에도 0개다.
재현     : CS-001 의 자해 문장이 validator 를 통과한다. 입력측 검사는 아예 존재하지 않는다.
영향     : 마이크가 열린 아동 서비스에서 아이가 실제 피해를 말하면 시스템이 롤플레잉을 계속한다.
           단일 최대 사고 벡터.
완화     : D2 로 STT 를 1단계에서 제외 → 아동 음성이 기기를 떠나지 않는다
완화붕괴 : STT 를 켜는 순간 즉시 붕괴한다
필요조치 : ✅ compliance/RISK-ESCALATION-POLICY.md 작성 +
           classifyChildUtterance() + 골든 픽스처 7건(S9-골든, 전부 통과).
           잔존: 호출부(STT 활성화 시), 보호자 알림 채널, 기록 보관 여부 결정
게이트   : G1(문서·픽스처) / G2-6(코드 분기)
상태     : 완료 (문서·분류기) / 보류 (호출부 — D2 로 유예)
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-008
심각도   : MED
분류     : [2] 기획 문서만  ← 승격 조건: 심사 제출·언론 노출 시 즉시 [1] 수준 리스크
영역     : 평판
근거     : README.md:73, :66, :54, :44, :11-13,16,18,65-68,71
자사규칙 : ETH-001(투명성), GOV-003-R2
현상     : "온라인 에듀테크 최초의", "관계/정서 서비스는 0원(0%)", "실시간 채점",
           "전문 심리상담/아동발달 가이드라인 체계화" + 통계 전건 무출처.
           :44 는 ETH-002:9-10, GDL-001:9-10,26 의 "팀 자체 정리 초안" 자백과 자기모순.
           팀이 루트 덱에서 "최초"를 철회했으나 **공개 GitHub 원격의 README 는 그대로**다.
재현     : README.md 를 열면 보인다
영향     : 검증 가능한 반례 1건으로 무너진다. :12 는 경쟁 카테고리 전체에 대한
           부정적 효능 주장이라 출처 없이 쓰면 가장 위험하다.
완화     : 없음
완화붕괴 : —
필요조치 : ✅ 완료 (2026-08-05, D4 범위 확장 승인) — 4건 수정 + §1 상단에 출처 표기
           상태 경고 블록 신설. 상세: compliance/upstream-patches/CHANGELOG.md 변경 #4
           잔존: 실제 출처(기관·보고서명·연도·URL) 확보는 팀 몫 — 수치를 지우지 않고
                 "미병기 상태"를 명시했다. 출처를 지어낼 수 없기 때문이다
게이트   : G1
상태     : 완료 (출처 확보만 잔존)
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-009
심각도   : MED
분류     : [2] 기획 문서만  ← 승격 조건: 정부지원사업 심사 제출 시 즉시 차단 사유
영역     : 규제
근거     : knowledge_base/government/GOV-001_정부지원사업_요구사항_TEMPLATE.md:14,20,21,23,24,28
자사규칙 : GOV-001-R1 (TODO 상태)
현상     : ":14 이 문서는 자리표시자(placeholder)입니다", ":28 [GOV-001-R1] (TODO)".
           공백: 콘텐츠 심의(:20), 국외 이전 조항(:21), 안전장치 증빙(:23), 금지 콘텐츠 목록(:24).
           status: draft 이라 프롬프트에도 미주입.
재현     : 문서를 열면 보인다
영향     : 심사 기준 그 자체다.
완화     : 없음
완화붕괴 : —
필요조치 : compliance/GOV-001-FILLED.md 에 채울 항목·출처·절차 정리 ✅.
           ⚠ **실제 공고문 원문이 필요하다** — 추측으로 채우면 잘못된 근거를 규칙 엔진에 주입한다.
           :23 안전장치 증빙은 compliance/CHILD-SAFETY-GATES.md 로 채운다.
           ⚠ active 승격 시 CS-010 이 동시에 터진다(후보 풀 9건 → 1건 탈락)
게이트   : G1
상태     : 보류 — 공고문 필요
```

```
[🚨 아동 규제/안전 리스크 감지]
ID       : CS-010
심각도   : MED
분류     : [1] 배포 코드 (CS-003 승격으로 경계에 도달)
영역     : 유해성·안전
근거     : rule_engine/rule_engine.py:21,34 · retriever.py:31-35
자사규칙 : GDL-001-R1, ETH-001-R4, GOV-001-R1
현상     : get_applicable_rules 의 top_k 기본값이 8인데 build_rule_sections(:34)가 top_k 를
           전달하지 않는다 → 호출자가 인자로 넘길 방법이 없다.
재현     : 활성 9건 - GOV-002(applies_to 불일치로 후보 탈락) = 후보 8건, top_k 8 → 현재 탈락 0.
           점수: LEGAL-001/002/003=3, GDL-002=3, ETH-001=2, ETH-002=2, GOV-003=1, GDL-001=1
영향     : 규칙이 하나만 더 active 가 되면 점수 최하위가 조용히 사라진다.
           다음 탈락 후보가 GDL-001 이며, 이는 만 4~6세 언어수준을 규정하는 유일한 문서다.
완화     : deps._apply_top_k_wrap() — in-process 로 top_k = max(8, 규칙수).
           /api/health 의 rules_dropped_by_top_k 가 감시.
완화붕괴 : 원본 저장소(수정하지 않음)를 직접 띄우면 그쪽은 여전히 top_k=8 이다.
           원본 데모를 병행 시연하면 규칙이 탈락한다.
필요조치 : ✅ 완료 — engine/rule_engine/rule_engine.py 에서 top_k 기본값을
           8 고정 → max(8, 활성 규칙 수) 로 직접 수정. in-process 래핑은 제거했다.
           /api/health 의 rules_dropped_by_top_k 가 계속 감시한다.
게이트   : G1 (CS-009 와 동시 처리 필요)
상태     : 완료 (BFF 경유 한정)
```

---

## 재현 실패로 강등한 항목

```
ID       : B-4
심각도   : LOW  (당초 MED 예측 → 재현 실패로 하향)
분류     : [1]
근거     : prompts/prompt_config.json:6 · rule_engine/validator.py:12
[가설]   : physical_boundary 는 라벨 자체가 "장난으로 치고 가기"이고 금지어 "때려"가 있어
           빈 배열 확률이 4개 중 가장 높을 것으로 예측했다.
재현     : ❌ 실패. physical_boundary 3회 + verbal_discomfort 4회, 총 7회 호출에서
           검증 거부 0건 (2026-08-05, 실제 키).
필요조치 : 슬라이스 카테고리로는 여전히 쓰지 않는다(예측이 틀렸어도 구조적 위험은 남는다).
           빈 배열 경로는 단위 테스트로 커버 — test_empty_scenarios_is_not_an_error.
           ⚠ 거부 0건이 CS-003 수정과 정합하나 **대조군이 없어 인과로 주장하지 않는다.**
           엄밀 검증: rules_index.json.orig 로 되돌린 대조군과 거부율 비교(Phase 2)
상태     : 관찰
```

---

## 요약

| 게이트 | 완료 | 보류 | 계 |
|---|---|---|---|
| G1 (데모 차단) | 6 | 5 | 11 |
| G2 (실아동 차단) | 0 | 11 | 11 |
| G3 (Phase 2 부채) | — | 6 | 6 |

**보류 5건의 성격**
- CS-002, CS-006 — 원본 규칙 엔진 내부. **상류 PR 권한 확인 필요**
- CS-008 — 원본 README. **팀이 직접 수정**
- CS-009 — **실제 공고문 필요**
- G1-11 — **D7(데모 바인딩 주소) 결정 대기**. `127.0.0.1` 유지면 G2 로 유예 가능
